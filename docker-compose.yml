services:
  cleaner:
    build: .
    ports:
      - "8021:8021"
    volumes:
      - ./uploads:/app/uploads
      - ./cleaned:/app/cleaned
    command: >
      sh -c "mkdir -p /app/uploads /app/cleaned &&
             python app.py"
